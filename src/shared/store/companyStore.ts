/**
 * Company Store (Legacy)
 * Re-exports from businessStore for backward compatibility
 * 
 * @deprecated Use useBusinessStore from './businessStore' instead
 */

export { 
  useCompanyStore, 
  useBusinessStore,
  companyStoreHelpers, 
  businessStoreHelpers 
} from './businessStore'; 